import React from "react";
import "./cart.css";
import { ItemSlot } from "../ItemSlot/Itemslot";

export const cart = () => {
  return (
    <div id="data_area">
      {/* {ApiRes.map(function (data, index) {
          return <ItemSlot data={data} key={index} country={country} />;
        })} */}
    </div>
  );
};
